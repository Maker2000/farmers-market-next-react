"use client"
import React from "react"

function AddToCart(data: { id: string; amount: number }) {
	return <button className="btn btn-primary flex-grow font-semibold uppercase">Add To Cart</button>
}

export default AddToCart
